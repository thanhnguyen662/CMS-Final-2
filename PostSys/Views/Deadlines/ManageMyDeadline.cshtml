@model IEnumerable<PostSys.Models.Deadline>

@{
	ViewBag.Title = "ManageMyDeadline";
}

<h2>ManageMyDeadline</h2>

@using (Html.BeginForm("ManageMyDeadline", "Deadlines", FormMethod.Get))
{
	<table class="table table-hover">
		<tr>
			<th>Deadline Name</th>
			<th>StartDate</th>
			<th>EndDate</th>
			<th>CreateBy</th>
			<th>Delete</th>
		</tr>
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Name</td>
				<td>@item.StartDate</td>
				<td>@item.EndDate</td>
				<td>@item.CreateBy</td>
				<td>@Html.ActionLink("Delete", "DeleteDeadline", new { id = item.Id })</td>
			</tr>
		}
	</table>
}
